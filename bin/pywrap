#!/usr/bin/env python

import os
import sys
import pywrap.cython as pycy


if __name__ == "__main__":
    if len(sys.argv) < 2:
        raise Exception("No filename given")

    filename = sys.argv[1]
    verbose = 3

    pycy.write_cython_wrapper(filename, verbose)
